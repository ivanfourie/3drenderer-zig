{
    "configurations": [
        {
            "name": "Linux",
            "includePath": [
                "${default}",
                // Location of SDL2 includes needed for the IDE
                // Use output of `pkg-config --cflags --libs sdl2` to find out yours
                // eg. -I/usr/include/SDL2 becomes /usr/include/SDL2/**
                "/usr/include/SDL2/**"
            ],
            "defines": [],
            "compilerPath": "/usr/bin/gcc",
            "cStandard": "c99",
            "cppStandard": "gnu++14",
            "intelliSenseMode": "linux-gcc-x64"
        }
    ],
    "version": 4
}